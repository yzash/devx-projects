version: '3.8'
services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: ceo_intelligence
      POSTGRES_USER: ceouser
      POSTGRES_PASSWORD: ceopass
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data:
